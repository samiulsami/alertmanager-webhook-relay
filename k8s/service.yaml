apiVersion: v1
kind: Service
metadata:
  name: alertmanager-webhook-relay
spec:
  selector:
    app.kubernetes.io/name: alertmanager-webhook-relay
  ports:
  - name: http
    port: 8080
    targetPort: http
